---
name: debugging
description: debugging — core reference for agents.
---

# debugging
Reproduce → isolate → inspect. Python `breakpoint()`/pdb, print/log. JS `console.log`/debugger + devtools. Read the FULL traceback bottom-up. Binary-search the code. Fix root cause not symptom.
