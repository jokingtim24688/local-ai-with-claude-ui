---
name: flask
description: flask — core reference for agents.
---

# flask
`Flask(__name__)`, `@app.route`, `request`, `jsonify`, `send_from_directory`. Run `app.run(host,port,threaded=True)`. Blueprints for structure. SSE: yield `data:...\n\n` with mimetype text/event-stream.
