---
name: async-concurrency
description: async-concurrency — core reference for agents.
---

# async-concurrency
Python: asyncio (async/await, gather, tasks) for IO; threading for IO too; multiprocessing for CPU. JS: promises, async/await, Promise.all. Avoid shared mutable state; use queues/locks.
