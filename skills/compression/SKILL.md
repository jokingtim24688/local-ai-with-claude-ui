---
name: compression
description: compression — core reference for agents.
---

# compression
zip: `zipfile`; tar/gz: `tarfile`; gzip single files. CLI `tar -czf x.tgz dir`, `unzip`. `shutil.make_archive`. For assets prefer zip on Windows, tar.gz on Linux.
