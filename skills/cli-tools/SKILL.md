---
name: cli-tools
description: cli-tools — core reference for agents.
---

# cli-tools
Args: argparse (stdlib) or `click`/`typer`. Pretty output: `rich` (tables, color, progress). Read stdin, exit codes, --help. Make tools composable (pipe-friendly).
