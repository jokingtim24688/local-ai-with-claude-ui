---
name: web-scraping
description: web-scraping — core reference for agents.
---

# web-scraping
`requests` + `beautifulsoup4` for static; `playwright`/`selenium` for JS-rendered. Parse with CSS selectors. Respect robots/rate limits. Set a User-Agent. Cache responses.
