---
name: markdown-templating
description: markdown-templating — core reference for agents.
---

# markdown-templating
Markdown for docs/READMEs. Jinja2 for text/HTML templates (`{{var}}`, `{% for %}`). Render configs, emails, pages. `markdown` lib to convert MD→HTML.
