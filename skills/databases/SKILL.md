---
name: databases
description: databases — core reference for agents.
---

# databases
SQLite (stdlib `sqlite3`, file-based, zero setup) for local apps. Postgres/MySQL for servers. SQL: SELECT/INSERT/UPDATE/DELETE, JOIN, WHERE, GROUP BY, indexes. Use parameterized queries (never string-concat). ORM: SQLAlchemy.
