---
name: env-secrets
description: env-secrets — core reference for agents.
---

# env-secrets
Never hardcode keys. Use env vars / `.env` (`python-dotenv`) and add .env to .gitignore. Read `os.environ`. For desktop apps store in the OS keyring (`keyring` lib).
