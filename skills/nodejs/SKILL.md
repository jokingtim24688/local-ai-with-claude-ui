---
name: nodejs
description: nodejs — core reference for agents.
---

# nodejs
Runtime for JS. npm/npx/pnpm. package.json scripts. fs/path/http/child_process modules. Express for servers. `npm install x`, `npm run dev`. ESM (`"type":"module"`) vs CommonJS require.
