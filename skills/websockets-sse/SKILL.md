---
name: websockets-sse
description: websockets-sse — core reference for agents.
---

# websockets-sse
SSE (one-way, simple): server yields `data:...\n\n`; browser `EventSource`. WebSocket (two-way): `websockets` (py) / `ws` (node) / browser `WebSocket`. Use SSE for streaming tokens.
