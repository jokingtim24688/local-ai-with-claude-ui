---
name: audio
description: audio — core reference for agents.
---

# audio
pydub (needs ffmpeg) for edit/convert; `sounddevice`/`pyaudio` for record/play; `mutagen` for tags. Music player uses these + a UI. ffmpeg CLI does almost anything.
