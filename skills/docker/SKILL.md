---
name: docker
description: docker — core reference for agents.
---

# docker
Dockerfile: FROM/COPY/RUN/CMD. `docker build -t x .`, `docker run -p 8080:80 x`, `docker exec -it x sh`, `docker compose up`. Volumes for data. Small base images (alpine/slim).
