---
name: video-ffmpeg
description: video-ffmpeg — core reference for agents.
---

# video-ffmpeg
ffmpeg: convert `-i in.mp4 out.webm`, trim `-ss -t`, scale `-vf scale=w:h`, extract audio `-vn`, frames `-vf fps=1`, concat, overlay. The swiss-army knife for media.
