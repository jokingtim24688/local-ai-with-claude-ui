---
name: datetime
description: datetime — core reference for agents.
---

# datetime
Python `datetime`, `time`, `zoneinfo` for TZ. ISO format. `time.time()` epoch. Never assume local TZ for storage — store UTC. `dateutil` for parsing.
