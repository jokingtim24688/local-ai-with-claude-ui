---
name: typescript
description: typescript — core reference for agents.
---

# typescript
Typed JS. `tsc --init`, compile `tsc`, run `ts-node f.ts`. Types: interface, type, generics `<T>`, unions `a|b`, enums. `strict` on. `as` casts sparingly. Great with React/Node.
