---
name: http-apis
description: http-apis — core reference for agents.
---

# http-apis
`requests` (py) / `fetch` (js). GET/POST/PUT/DELETE, headers, JSON body, query params, status codes. Auth: Bearer token header. Handle timeouts + retries. Read the API docs; respect rate limits.
