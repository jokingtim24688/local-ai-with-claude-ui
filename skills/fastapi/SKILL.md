---
name: fastapi
description: fastapi — core reference for agents.
---

# fastapi
Async API framework. `@app.get/post`, Pydantic models for validation, automatic docs at /docs. Run `uvicorn main:app --reload`. Dependency injection with Depends.
