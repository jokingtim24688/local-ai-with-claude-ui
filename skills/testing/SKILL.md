---
name: testing
description: testing — core reference for agents.
---

# testing
Python `pytest` (test_*.py, assert), fixtures, `pytest -q`. JS `jest`/`vitest`. Test behavior not internals. Arrange-Act-Assert. Mock external calls. Run tests before every push.
