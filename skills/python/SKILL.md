---
name: python
description: python — core reference for agents.
---

# python
Core: pathlib, os, sys, subprocess, json, re, dataclasses, typing, itertools, collections. venv: `python -m venv .venv`. Format `black`, lint `ruff`. Run `python -m mod`. f-strings, comprehensions, context managers, generators. Prefer `subprocess.run([...],capture_output=True,text=True)`.
