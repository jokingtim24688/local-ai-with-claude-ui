---
name: bash
description: bash — core reference for agents.
---

# bash
vars `X=1` use `$X`. `if [ ... ]; then; fi`, `for x in ...; do; done`, `while`. Pipes `|`, redirect `> >> 2>&1`. `$(cmd)` subshell. `set -e` fail fast. functions. test files `[ -f x ]`.
