---
name: linux-admin
description: linux-admin — core reference for agents.
---

# linux-admin
apt/dnf install, systemctl start/enable, journalctl -u, chmod/chown, ps/top/htop, df/du, cron (`crontab -e`), ufw firewall, ssh, users (useradd), tar/gzip. sudo for privilege.
