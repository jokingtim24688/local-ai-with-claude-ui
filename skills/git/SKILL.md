---
name: git
description: git — core reference for agents.
---

# git
status/add/commit/push/pull/branch/checkout -b/merge/rebase/stash/log --oneline/diff/restore/reset. `git remote -v`. Resolve conflicts by editing `<<<<`. Never force-push shared branches. `.gitignore` for build/venv.
