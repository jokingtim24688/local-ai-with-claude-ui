---
name: json-yaml
description: json-yaml — core reference for agents.
---

# json-yaml
JSON: `json.load/dumps(indent=2)`. YAML: `pip install pyyaml`; `yaml.safe_load`. YAML = indentation, no tabs. Use JSON for APIs, YAML for config. Validate with a schema when it matters.
