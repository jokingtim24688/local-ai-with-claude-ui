---
name: regex
description: regex — core reference for agents.
---

# regex
`.` any, `*`+`?` quantifiers, `\d\w\s`, `[abc]`, `^$` anchors, `()` groups, `(?:)` non-capture, `|` alt, `\b` word boundary. Python `re.search/findall/sub`. Test small. Escape literals.
