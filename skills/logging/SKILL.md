---
name: logging
description: logging — core reference for agents.
---

# logging
Python `logging` (getLogger, levels DEBUG/INFO/WARNING/ERROR). Don't print in libraries. Structured logs (key=value/JSON) for tools. Log inputs, decisions, errors with context.
